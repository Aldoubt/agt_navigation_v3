# 导航启动文档

本文只记录当前现场导航启动入口。所有命令都假定工作区为
`/home/yangxuan/ros2_ws`，地图使用当前已存在的显式资产：

```text
/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed
```

不要依赖未设置的 `AGT_MAP_ROOT`。RPP、MPPI、FAST-LIO2 anchor 是三套互斥的
导航链；同一 ROS domain 内一次只能启动其中一套。

## 0. 车端构建与环境

首次使用新代码、或切换到 FAST-LIO2 anchor 模式前执行：

```bash
cd /home/yangxuan/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install --base-paths src/agt_navigation_v3
source /home/yangxuan/ros2_ws/install/setup.bash
```

其余每个新终端均先执行：

```bash
source /opt/ros/humble/setup.bash
source /home/yangxuan/ros2_ws/install/setup.bash
```

## 1. 启动传感器、底盘和静态 TF

此终端启动 MID360、Bunker CAN、robot description、RTK 驱动和相机云台。
`enable_rtk_manager:=false` 很重要：导航终端会启动唯一的 RTK manager，避免
重复节点。

```bash
ros2 launch agt_system_bringup sensor_session.launch.py \
  bunker_can_port:=can0 \
  enable_rtk_manager:=false
```

启动后确认：

```bash
ros2 topic hz /livox/lidar
ros2 topic hz /livox/imu
ros2 topic hz /wheel/odom
```

## 2. Batch-LIO + RPP 导航（当前基线）

该命令启动 Batch-LIO、Batch-LIO adapter、3D-BBS/GICP 自动重定位、
Localization Manager、Nav2 RPP、cmd_vel guard 与 RViz。

```bash
ros2 launch agt_system_bringup rviz_field_demo.launch.py \
  map:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/navigation/map.yaml \
  global_map:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/localization/global_map.pcd \
  relocalization_assets:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/localization/relocalization \
  map_id:=bunker_mid360_v003_rpp \
  nav2_params_file:=/home/yangxuan/ros2_ws/src/agt_navigation_v3/navigation/nav2/agt_nav2_bringup/config/nav2_params_rpp.yaml
```

## 3. Batch-LIO + MPPI 导航

先完全停止上一节的 RPP launch，再运行此命令。除 Nav2 controller 参数文件外，
其余链路与 RPP 相同。

```bash
ros2 launch agt_system_bringup rviz_field_demo.launch.py \
  map:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/navigation/map.yaml \
  global_map:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/localization/global_map.pcd \
  relocalization_assets:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/localization/relocalization \
  map_id:=bunker_mid360_v003_mppi \
  nav2_params_file:=/home/yangxuan/ros2_ws/src/agt_navigation_v3/navigation/nav2/agt_nav2_bringup/config/nav2_params_mppi.yaml
```

## 4. FAST-LIO2 + one-shot 3D-BBS anchor + 固定 RPP

此模式只启动 FAST-LIO2 作为连续局部里程计。3D-BBS/GICP 在启动静止阶段自动
成功一次后建立 `map -> odom`；不会启动 Batch-LIO、PGO 或 `agt_map_tracker`，
不会有低频 map-tracking 修正打断导航。

```bash
ros2 launch agt_system_bringup fastlio_anchor_nav.launch.py \
  map:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/navigation/map.yaml \
  global_map:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/localization/global_map.pcd \
  relocalization_assets:=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed/localization/relocalization
```

详细 frame 约定见 [FASTLIO_ANCHOR_NAVIGATION.md](docs/architecture/FASTLIO_ANCHOR_NAVIGATION.md)。

## 5. 导航允许运动前检查

完成全局重定位、且机器人保持静止后执行：

```bash
ros2 run tf2_ros tf2_echo odom base_link
ros2 run tf2_ros tf2_echo map odom
ros2 topic echo --once /agt/localization/metrics
ros2 topic echo --once /agt/localization/status
```

必须满足：

- `odom -> base_link` 与 `map -> odom` 均持续可查；
- metrics `state` 为 `LOCALIZED` 或 `TRACKING`；
- 再通过 RViz 的 **Nav2 Goal** 发送短距离目标。

FAST-LIO2 anchor 模式还应确认没有误启动旧链：

```bash
ros2 node list | grep -E 'batch|pgo|map_tracker'
```

该命令应无输出。

## 6. 录制导航诊断 bag

重定位完成后启动录包，先静止 20 秒，再低速直行、左右转向并发送一个短距离
Nav2 Goal；建议总时长至少 2 分钟。将输出目录中的名称改为本次测试标识。

```bash
ros2 bag record \
  -o /home/yangxuan/ros2_ws/experiments/results/rosbag/nav_diagnostic_YYYYMMDD_HHMMSS \
  /livox/lidar \
  /livox/imu \
  /wheel/odom \
  /aft_mapped_to_init \
  /fastlio2/lio_odom \
  /agt/odometry/local \
  /agt/odometry/adapter_status \
  /tf \
  /tf_static \
  /agt/relocalization/pose \
  /agt/relocalization/status \
  /agt/localization/status \
  /agt/localization/metrics \
  /plan \
  /path \
  /local_plan \
  /cmd_vel_nav \
  /cmd_vel \
  /cmd_vel_smoothed \
  /mux/cmd_vel \
  /compute_path_to_pose/_action/status \
  /follow_path/_action/status \
  /rosout
```

录包前先检查 `/agt/relocalization/pose` 类型；它必须只有一种：

```bash
ros2 topic list -t | grep '/agt/relocalization/pose'
ros2 topic info -v /agt/relocalization/pose
```

正确类型为 `geometry_msgs/msg/PoseWithCovarianceStamped`。若出现多个类型，先
停止错误的 RViz/遗留节点后再录包。
