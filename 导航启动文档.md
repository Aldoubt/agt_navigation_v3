# 导航启动文档

顶层只保留四个入口：

```text
hardware.launch.py
localization.launch.py
navigation.launch.py
debug.launch.py
```

所有终端先执行：

```bash
cd /home/yangxuan/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
```

地图示例：

```bash
MAP_ROOT=/home/yangxuan/ros2_ws/maps/bunker_mid360_mapping_20260901_205036/v003-indexed
```

## 1. 硬件

```bash
ros2 launch agt_system_bringup hardware.launch.py \
  bunker_can_port:=can0
```

该入口唯一启动 `robot_state_publisher`，同时启动 MID360、Bunker、RTK 和相机云台。
按需用 `enable_*:=false` 关闭不在场的设备。Bunker 的 `publish_odom_tf` 被强制关闭。

## 2. 定位

```bash
ros2 launch agt_system_bringup localization.launch.py \
  global_map:=$MAP_ROOT/localization/global_map.pcd \
  relocalization_assets:=$MAP_ROOT/localization/relocalization
```

该入口启动 Batch-LIO、格式桥、全局重定位，以及唯一的
`/agt_localization_manager`。它独占 `map -> odom`。

## 3. 导航

```bash
ros2 launch agt_system_bringup navigation.launch.py \
  map:=$MAP_ROOT/navigation/map.yaml \
  map_id:=bunker_mid360_v003
```

该入口启动 Nav2、命令安全门和唯一的 `/agt_pointcloud_preprocessor`。点云输出固定为
`/agt/navigation/points_obstacles`。

## 4. 调试（可选）

```bash
ros2 launch agt_system_bringup debug.launch.py
```

调试入口仅启动 RViz 和只读 preflight，不会重复启动硬件、定位或点云 owner。

## 5. 静态验收

```bash
cd /home/yangxuan/ros2_ws/src/agt_navigation_v3
scripts/check_runtime.sh
scripts/check_tf.sh
scripts/check_topics.sh
scripts/check_tf_publishers.sh /tmp/tf_authority.json
scripts/check_topic_rates.sh /tmp/topic_rates.json
```

预期关键节点各出现一次：

```text
/agt_localization_manager
/robot_state_publisher
/agt_pointcloud_preprocessor
```

目标 TF 链：

```text
map -> odom -> base_footprint -> base_link -> lidar_link
```

详细静态审计见 [tf_audit_report.md](tf_audit_report.md)。

## 6. MID360 + Bunker 实机验收

```bash
cd /home/yangxuan/ros2_ws/src/agt_navigation_v3
ACCEPTANCE_DURATION_SEC=180 scripts/run_hardware_acceptance.sh
```

该工具链只观察、检查和录包，不发布速度、目标点或 TF。运动中心窗口和导航录制窗口
内的动作必须由现场操作员通过现有 Nav2 安全控制链执行。完整的场地准备、操作顺序、
判据和证据清单见
[docs/HARDWARE_ACCEPTANCE_MID360_BUNKER.md](docs/HARDWARE_ACCEPTANCE_MID360_BUNKER.md)。

## 7. 云台任务、图片目录与 5/7/10 m 点位

云台三视角模板、逐点任务 YAML、图片命名与默认归档目录，以及精确生成
5/7/10 m 米制目标的方法见
[云台相机任务与导航点设置说明.md](云台相机任务与导航点设置说明.md)。

RViz 的 `map` 坐标本身就是米制；鼠标点选只是没有默认显示数值。正式导航精度验收不要依赖鼠标点位，使用坐标生成器和地面物理测量。
