# 导航启动文档（V4）

> 改造总目标、跨电脑部署步骤和 `src` 预期目录见 [工作空间改造收敛与部署启动方案](docs/upgrade/WORKSPACE_CONSOLIDATION_PLAN.md)。本文保留现有启动命令；统一方案明确区分当前能力与待完成目标。

> 状态：V4 仍在软件与现场验收阶段，当前结论为 `NOT_READY_FOR_FIELD_ACCEPTANCE`。以下实机启动顺序用于验收门通过后的现场操作；现在先做不接硬件的检查。判据见 [V4 验收矩阵](docs/acceptance/V4_ACCEPTANCE_MATRIX.md)，实际证据见 [迁移报告](V4_MIGRATION_REPORT.md)。

## 1. 三个部分各做什么

| 部分 | 职责 | 启动方式 |
| --- | --- | --- |
| 后台运行栈 | 依次启动硬件、单一 LIO、全局定位、Nav2、Navigation Health 与 Capability，并在退出时有序停止 | `scripts/run_field_stack.sh` |
| RViz | 查看地图、TF、点云、代价地图、定位和实际轨迹；画线工具可用于调试 `FollowRoute` | 后台加 `--rviz`，它不是整车启动器 |
| HMI | 最终上位控制界面，展示状态并通过 Navigation/Mission Action 提交目标或任务 | 后台达到 `NAV_READY` 后，在另一个终端单独启动 |

HMI 不直接启动驱动、LIO、Nav2，也不接管 `map → odom` 或底盘速度。Navigation Health 不就绪时，HMI 不应提交目标。HMI 的界面请求、状态和取消语义仍需完成 [A7 验收](docs/acceptance/V4_ACCEPTANCE_MATRIX.md)。

## 2. 当前可做：不接硬件核对地图与配置

```bash
cd /home/yangxuan/ros2_ws
src/agt_navigation_v3/scripts/run_field_stack.sh \
  --mode navigation --map auto --robot bunker_v1 --dry-run
```

`auto` 根据 `<工作空间>/maps/registry.yaml` 选择默认的已验证版本。目前本机解析为 `bunker_mid360/20260924-trav-integration-v1`；以命令实际打印的 `map_id`、`map_version`、`map_registry`、导航图、PCD 和重定位资源路径为准。`active` 指针目前为空，历史 `active_map.yaml` 指向另一个旧版本，不能把两者当成同一选择。

要显式检查其他已注册版本，可加 `--map map_id/map_version`。地图数据不在源码仓库内；换电脑时把正式 Map Package 和 registry 放到新工作空间的 `maps/`，或用 `--map-registry /绝对路径/registry.yaml` 指定。离线 RViz 画线预览也要传同一 registry：

```bash
ros2 launch agt_rviz_patrol path_tool_offline.launch.py \
  map:=auto robot:=bunker_v1 \
  map_registry:=/home/yangxuan/ros2_ws/maps/registry.yaml
```

离线预览只检查地图显示与画线交互，不启动机器人，也不证明定位或路径可执行。

### 默认读取的目录（2026-09-24 实测）

- 注册表：`/home/yangxuan/ros2_ws/maps/registry.yaml`。
- 当前地图包：`/home/yangxuan/ros2_ws/maps/bunker_mid360/20260924-trav-integration-v1`。
- 二维图：地图包内 `navigation/bunker_v1/map.yaml`（引用同目录 `map.pgm`）。
- 定位点云：地图包内 `localization/global_map.pcd`。
- 重定位资产：地图包内 `localization/relocalization/`。

默认根目录按启动脚本所在工作空间计算，不是仓库内部的 maps。`AGT_MAP_REGISTRY` 可覆盖默认注册表，命令行 `--map-registry` 优先于环境变量。`auto` 跟随 registry 的 `default_map_id` 和 `default_map: latest_validated`，不是按文件时间找最新；以后发布同一 map ID 的已验收版本后，下一次启动会读新指针，正在运行的导航不热切换。

2026-09-24 后续发布：已按用户确认将 traversability 版本 `20260924-trav-integration-v1` 发布为默认正式地图，用于下午受控测试；包内记录 `field_acceptance: pending`，不等于完成实车验收。详见 [地图发布与修正逻辑](docs/mcp-map-release-and-correction-20260924.md)。当前 `enable_map_tracking=false`，默认是启动全局锚定后依赖 FAST-LIO2，不持续进行地图匹配纠漂。

默认 FAST-LIO2 使用 `install/agt_navigation_runtime/share/agt_navigation_runtime/config/fastlio2_mid360_navigation.yaml`，原始里程计 `/fastlio2/lio_odom` 经 `agt_fastlio_adapter` 输出 `/agt/odometry/local`。不要把 Batch-LIO 的外参符号复制进 FAST 配置。

### 初始化定位模式：自动失败后人工保底

保持原默认 `--localization-mode auto`：最多两次自动全局定位，失败退出。
下午受控测试可显式选择 `auto_then_manual`；也可选择 `manual` 跳过自动搜索。

先做无运动检查：

```bash
src/agt_navigation_v3/scripts/run_field_stack.sh \
  --mode navigation --map auto --localization-mode auto_then_manual --rviz --dry-run
```

现场前置检查通过后去掉 `--dry-run`。自动两次失败时，后台保留硬件/LIO，打开初始化地图窗口，
但**不启动 Nav2**。保持车辆静止，在 RViz 用 **2D Pose Estimate** 指定 base_link 大概位置与朝向；
人工种子经局部 GICP 精配准及定位管理器校验通过后，才继续原有导航启动流程。
失败则继续等待，不会把鼠标点选直接视为有效定位；Ctrl+C 有序退出。

现场脚本会把每次自动重定位的查询点云、可复现的后端命令和结果保存在本轮日志的 `relocalization_queries/`。若自动匹配被拒绝，保留该目录与 `localization.log` 供离线分析；不要仅凭一次低分结果放宽 fitness 门限。

没有 `--rviz` 时可用远程 RViz：地图 `/agt/initialization/map`，初始位姿 `/initialpose`，Fixed Frame=`map`。
完整步骤、门限和验证范围见 [人工定位保底说明](docs/mcp-manual-initialization-fallback.md)。
持续地图跟踪纠偏仍默认关闭，本功能只处理初始化保底。

## 3. 验收通过后的现场启动顺序

先按 [V4 验收矩阵](docs/acceptance/V4_ACCEPTANCE_MATRIX.md)完成构建、SDK 版本、ROS 图、地图及现场门。所有终端使用同一 ROS 2 Humble 和同一工作空间的 `install/setup.bash`、`ROS_DOMAIN_ID`。

首次从拆分后的 `agt_robot_platform` 和 `agt_robot_description` 构建，或遇到 `robot_hardware.launch.py` 指向旧 `src/agt_robot_bringup` 路径时，先在无导航进程运行时重建迁移的包：

```bash
cd /home/yangxuan/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --packages-select ugv_sdk bunker_msgs bunker_base \
  agt_robot_description agt_robot_bringup \
  --symlink-install --cmake-clean-cache
source install/setup.bash
ros2 launch agt_system_bringup hardware.launch.py --show-args
```

最后一条命令只检查 launch 能解析，不会启动硬件。若此前安装目录仍有指向旧源码位置的符号链接，`--cmake-clean-cache` 会让这些包按新源码路径重新安装。

**终端 A：后台与 RViz 调试**

```bash
cd /home/yangxuan/ros2_ws
src/agt_navigation_v3/scripts/run_field_stack.sh \
  --mode navigation --map auto --robot bunker_v1 \
  --rviz  # 默认 FAST-LIO2，可显式加 --lio-backend fastlio2
```

2026-09-24 起，脚本与 `localization.launch.py` 默认均为 FAST-LIO2，不再默认使用 Batch-LIO。只有需要对照或回退时才显式传 `--lio-backend batch_lio`；先正常关闭旧栈，再在车辆静止时重新启动与定位。只在脚本完成 `DEMO PREFLIGHT PASS`、Navigation Health 连续 `NAV_READY`，且 RViz 中地图与机器人位姿正确后进入操作测试。不要同时启动第二套硬件或 LIO。

导航的运动判据使用 `/agt/odometry/local`（默认 FAST-LIO2）：包括启动重定位的静止判定、Navigation Health 的里程计检查和 Nav2 控制器输入。`/wheel/odom` 仍由 Bunker 驱动发布，可在 RViz 查看、录包比较，但其缺失或质量变化不阻断导航启动、路线预览或路线执行。底盘驱动、遥控优先权及原有速度命令保护仍按硬件链工作。

**局部实时避障关闭对照试验**：

```bash
cd /home/yangxuan/ros2_ws
src/agt_navigation_v3/scripts/run_field_stack.sh \
  --mode navigation --map auto --robot bunker_v1 \
  --no-local-obstacle-avoidance --rviz
```

该开关仅对本次运行生效：局部代价地图不加载实时点云 `voxel_layer`，仍加载静态地图和膨胀层；全局地图与规划保持原配置。局部动态障碍不会触发避让。仅在清空人员和移动障碍的受控区域低速对照测试，保留遥控接管，结束后重启普通命令恢复默认配置。

**终端 B：HMI 上位控制**

```bash
cd /home/yangxuan/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run agt_robot_hmi agt_robot_hmi
```

HMI 单点导航走 `/navigation/navigate_to`，任务路线走 `/mission/execute_route`。纯 `--mode navigation` 不启动 Mission Runtime；路线或拍照任务通过外部 `agt_mission_bringup` 入口。目前 `--mode inspection` 同时启用 V4 Mission Runtime 和显式旧巡检兼容链，尚不能作为新 HMI 任务闭环的现场 PASS。完成 A5–A7 的 Action、暂停/取消和界面联动验收后再用 HMI 控制实车。

## 4. RViz 调试边界

RViz 保留为调试终端：观察 `/map`、`map → odom → base_link`、全局与局部代价地图、障碍点云、规划路径，以及 LIO/轮速实际轨迹。画线工具通过 `/navigation/follow_route` 进入 V4 Capability。RViz 自带的 `Nav2 Goal` 直接发送 `/navigate_to_pose`，会绕过 V4 Capability 的 Health 门控；其结果只能用于受控的底层 Nav2 调试，不能算作 V4 上位控制验收。旧 `/agt/rviz_patrol/start` 巡检链仍是兼容入口，也不能替代新 Mission 验收。

若地图不显示，先核对 registry 解析、`/map` 发布和 RViz Fixed Frame=`map`；不要从旧实验目录直接指定单张 `map.yaml`。若定位不稳或障碍残影，先保存本次日志与 bag，再对照 [历史排障记录](docs/archive/导航启动文档_V3_20260923.md)。

## 5. 停止与迁移

先停止任务并确认底盘静止，再在终端 A 按一次 `Ctrl+C`，等待脚本有序关闭；不要直接断电。运行日志在 `~/.ros/agt_field_stack/<timestamp>/`。

Mission 包位于独立的 `agt_mission` 仓库；Robot Bringup 与本机使用的 Bunker/UGV SDK 包位于独立的 `agt_robot_platform` 仓库。导航仓库只保留 ROS 包依赖、启动编排和精确 SHA 的 [外部依赖清单](dependencies/navigation_v4_external.repos)。其他源码仓库、正式地图和本机 SDK 仍需按 [V4 跨机器迁移说明](docs/v4_migration/portable_workspace.md)单独核对。完整 V4 工作空间尚未在新机器干净克隆与构建，不应宣称整体迁移已通过。
