# Third-party boundary

Pinned upstream ROS repositories remain workspace dependencies and are recorded
under `dependencies/`. They are not renamed as AGT functional-domain packages
by this refactor.
