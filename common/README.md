# Common

This directory is intentionally empty of runtime abstractions. Shared code is
introduced only when a concrete cross-domain contract requires it; this avoids
creating a second manager or base framework during the functional-domain
refactor.
