# 云台相机任务与导航点设置说明

## 1. 本仓库的视觉边界

本仓库只负责：导航到点、确认底盘停止、控制云台到位、获取到位后的新图、保存图片和位姿元数据。

本仓库不负责 YOLO、花果识别、数量统计或 mAP50 计算。识别程序应离线读取本仓库归档的图片；不要把识别节点并入导航运行链。

任务链为：

```text
NavigateToPose -> 实测停止 -> AcquireView -> 新图 -> 图片/位姿/RTK/云台角度归档
```

## 2. 云台任务在哪里修改

### 2.1 RViz 点选巡检：统一三视角

默认模板是：

```text
navigation/nav2/agt_rviz_patrol/config/front_sky_three_views.yaml
```

默认角度为左前上、正前上、右前上。`heading`、`roll`、`pitch` 和 `tolerance` 的单位均为度。实机首次运行必须确认 pitch 正负方向；不要仅凭画面名称推断方向。

RViz 巡检适配器当前要求模板中恰好有三个 `views`。建议复制模板后修改，不直接改安装目录：

```bash
mkdir -p /home/yangxuan/camera_presets
cp navigation/nav2/agt_rviz_patrol/config/front_sky_three_views.yaml \
  /home/yangxuan/camera_presets/orchard_three_views.yaml

ros2 param set /rviz_patrol preset_file \
  /home/yangxuan/camera_presets/orchard_three_views.yaml
```

参数在下一次调用 `/agt/rviz_patrol/start` 时读取。模板示例：

```yaml
base_settle_time: 2.0
views:
  - tag: left
    heading: -45.0
    roll: 0.0
    pitch: 35.0
    tolerance: 1.5
    timeout: 8.0
    stable_samples: 3
    settle_time: 0.8
    image_timeout: 2.0
    save_image: true
    required: true
  # 继续配置 center、right；总数必须为 3
```

履带振动场景先增加 `base_settle_time` 和每个视角的 `settle_time`，不要先放宽到位容差。C1 驱动机械范围为 heading `[-160°, 160°]`、roll `[-40°, 40°]`、pitch `[-90°, 90°]`。

### 2.2 每个导航点使用不同视角

直接复制并修改：

```text
navigation/nav2/agt_navigation_runtime/config/mission_example.yaml
```

每个点的 `views` 可以不同，也可以为空。例如：

```yaml
version: 1
mission_id: orchard_row_a
map_id: orchard_map_v1
points:
  - id: TREE_001
    pose: {x: 3.20, y: -1.40, yaw: 0.0, frame_id: map}
    settle_time: 2.0
    views:
      - {tag: left_low, heading: -35.0, roll: 0.0, pitch: 10.0,
         tolerance: 1.5, timeout: 8.0, stable_samples: 3,
         settle_time: 0.8, image_timeout: 2.0, save_image: true, required: true}
      - {tag: center_high, heading: 0.0, roll: 0.0, pitch: 40.0,
         tolerance: 1.5, timeout: 8.0, stable_samples: 3,
         settle_time: 0.8, image_timeout: 2.0, save_image: true, required: true}
```

执行：

```bash
MISSION_FILE=/absolute/path/orchard_row_a.yaml
ros2 action send_goal /agt/mission/execute \
  agt_robot_interfaces/action/ExecuteInspectionMission \
  "{mission_file: '$MISSION_FILE', resume: false}" --feedback
```

## 3. 图片格式和默认保存位置

C1 capability 的原始图片为 PNG：

```text
~/autolabor_c1_capture/YYYYMMDD/<tag>_<sec>_<nanosec>.png
```

巡检任务会把成功图片硬链接或复制到任务归档，识别程序应读取这个最终目录：

```text
~/.ros/agt_inspection_records/
  <mission_id>_<YYYYMMDD_HHMMSS>/
    mission.yaml
    manifest.json
    captures.csv
    captures.jsonl
    images/
      <point_id>/
        <view_tag>.png
```

归档保持 C1 返回文件的后缀；当前 C1 默认返回 `.png`。`captures.csv/jsonl` 同时记录图片时间戳、`map -> base_link` 位姿、邻近 RTK 数据和实际云台角度。

另有调试服务 `/capability/camera/capture`，默认保存到 `~/.ros/agt_navigation_debug/captures`，它不是正式多角度任务的归档入口。

现场检查：

```bash
ros2 action list -t | grep camera_gimbal
ros2 topic hz /cv_camera0/image_raw
ls -dt ~/.ros/agt_inspection_records/* | head -1
```

## 4. RViz 点位是不是米制

是。ROS 的 `map` 坐标使用 SI 单位，`position.x/y` 都是米；`map.yaml` 的 `resolution` 负责像素到米的换算。RViz 的 `2D Goal Pose` 只是用鼠标产生米制 `PoseStamped`，画面不默认显示数字。

点击目标时可直接查看米制坐标：

```bash
ros2 topic echo /goal_pose geometry_msgs/msg/PoseStamped --once
```

RViz 工具栏的 `Measure` 可量距离，但鼠标选点不适合作为 5/7/10 m 精度验收真值。正式测试应使用精确坐标和卷尺/激光测距的地面标记。

## 5. 精确生成 5/7/10 m 测试点

先将车放在实测起点，通过 TF 读取起点的 `x`、`y` 和 RPY 中的 yaw：

```bash
ros2 run tf2_ros tf2_echo map base_footprint
```

将起点坐标和测试方向输入生成器。方向角以 `map +X` 为 0°，`map +Y` 为 90°：

```bash
cd /home/yangxuan/ros2_ws/src/agt_navigation_v3
tools/hardware_acceptance/generate_distance_missions.py \
  --map-id orchard_map_v1 \
  --start-x 1.250 --start-y -0.800 --heading-deg 30.0 \
  --distances 5 7 10 --repeats 5 \
  --output-dir ~/.ros/agt_distance_acceptance
```

输出：

```text
distance_5m.yaml
distance_7m.yaml
distance_10m.yaml
targets.csv
```

每个 YAML 按“目标点 -> 起点”往返五次，目标点默认停留 15 秒供现场测量。分别执行：

```bash
MISSION_FILE=$HOME/.ros/agt_distance_acceptance/distance_5m.yaml
ros2 action send_goal /agt/mission/execute \
  agt_robot_interfaces/action/ExecuteInspectionMission \
  "{mission_file: '$MISSION_FILE', resume: false}" --feedback
```

5、7、10 m 是起点到目标点的欧氏距离，不是机器人实际轨迹长度。验收误差必须以地面物理测量为准，Nav2/TF 只作为辅助证据。

## 6. 当前 Nav2 精度基线

当前参数已收紧为：

```text
planner tolerance       0.05 m
xy goal tolerance       0.08 m
yaw goal tolerance      0.0872665 rad（5°）
progress translation    0.05 m
progress rotation       0.05 rad
progress timeout        15 s
```

这些值不等于系统已经达到 12/15/20 cm 实机精度。必须完成每个距离五次物理测量；若临近目标振荡，应先检查定位偏差、履带打滑和控制器终端行为，不要直接放宽容差。

## 7. 起伏地面与树干场景的感知基线

唯一节点和输出保持不变：

```text
/agt_pointcloud_preprocessor -> /agt/navigation/points_obstacles
```

地面滤波已改为径向坡度跟踪，默认允许跟踪不超过 35° 的连续坡面；台阶、树干和超过坡度阈值的突变保留为障碍。后方扇区删除默认关闭，避免漏掉后方树干；体素降采样为 0.10 m。

实机首轮必须标定 `config/perception.yaml` 中的 `sensor_ground_z_m`，它表示 `base_link` 原点到地面的 Z 高度（当前基线为 `-0.20 m`）。该升级不检测负障碍/深坑，也不是完整的可通行性高程图，因此沟坎场地仍需人工限区与低速测试。
