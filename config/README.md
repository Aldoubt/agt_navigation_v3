# Configuration notes

`pointcloud_filter.yaml` is the starting configuration for the tilted MID360 and chassis self-filter.

`localization.yaml` defines the global localization manager and deliberately leaves map/database paths empty until a real rosbag is benchmarked.

Do not freeze external dependency branches yet. Record repository URLs and tested commits only after the end-to-end V1 benchmark is stable.
